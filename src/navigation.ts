// Navigation is sourced from src/data/navigation.json so it can be edited in
// CloudCannon's data editor. Hrefs are stored as resolved site paths.
import navigation from '~/data/navigation.json';

export const headerData = navigation.header;
export const footerData = navigation.footer;
